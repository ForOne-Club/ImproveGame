目前并不支持由模组开发者自己制作的某个选项的交互界面
但是会逐步分解至最基本的类型以及它们相应的设置项

目前已知的问题：
1、Example mod的ShowCaseA:DataTypes的CustomKey会导致崩溃
   (Example mod自身似乎写入有问题，而我这里是成功写入了然后在读取炸了)
2、Color等右键恢复默认值可能失效或者崩溃

以后也许会出模组作者自己排版一个配置界面的文档